name: agentic-rag-platform

services:
  postgres:
    image: postgres:18.4-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-agentic_rag}
      POSTGRES_USER: ${POSTGRES_USER:-agentic_rag}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agentic_rag_dev_only}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks: [backend]

  chroma:
    image: chromadb/chroma:1.5.9
    profiles: ["rag"]
    ports:
      - "${CHROMA_EXPOSED_PORT:-8001}:8000"
    volumes:
      - chroma_data:/data
    restart: unless-stopped
    networks: [backend]

  ollama:
    image: ollama/ollama:0.31.1
    profiles: ["rag"]
    ports:
      - "${OLLAMA_PORT:-11434}:11434"
    volumes:
      - ollama_data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s
    restart: unless-stopped
    networks: [backend, model_egress]

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: development
    environment:
      APP_NAME: ${APP_NAME:-agentic-rag-backend}
      APP_VERSION: ${APP_VERSION:-0.1.0}
      APP_ENV: ${APP_ENV:-development}
      APP_DEBUG: ${APP_DEBUG:-false}
      DOCS_ENABLED: ${DOCS_ENABLED:-true}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-agentic_rag}:${POSTGRES_PASSWORD:-agentic_rag_dev_only}@postgres:5432/${POSTGRES_DB:-agentic_rag}
      DATABASE_ECHO: ${DATABASE_ECHO:-false}
      DATABASE_POOL_SIZE: ${DATABASE_POOL_SIZE:-5}
      DATABASE_MAX_OVERFLOW: ${DATABASE_MAX_OVERFLOW:-10}
      DATABASE_POOL_TIMEOUT_SECONDS: ${DATABASE_POOL_TIMEOUT_SECONDS:-30}
      DATABASE_POOL_RECYCLE_SECONDS: ${DATABASE_POOL_RECYCLE_SECONDS:-1800}
      DATABASE_HEALTHCHECK_TIMEOUT_SECONDS: ${DATABASE_HEALTHCHECK_TIMEOUT_SECONDS:-2}
      UPLOAD_ROOT: /app/data/uploads
      MAX_UPLOAD_SIZE_BYTES: ${MAX_UPLOAD_SIZE_BYTES:-10485760}
      CHUNK_SIZE_CHARS: ${CHUNK_SIZE_CHARS:-1200}
      CHUNK_OVERLAP_CHARS: ${CHUNK_OVERLAP_CHARS:-200}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://ollama:11434}
      OLLAMA_EMBED_MODEL: ${OLLAMA_EMBED_MODEL:-qwen3-embedding:0.6b}
      EMBEDDING_DIMENSION: ${EMBEDDING_DIMENSION:-1024}
      EMBEDDING_BATCH_SIZE: ${EMBEDDING_BATCH_SIZE:-32}
      EMBEDDING_REQUEST_TIMEOUT_SECONDS: ${EMBEDDING_REQUEST_TIMEOUT_SECONDS:-30}
      CHROMA_HOST: ${CHROMA_HOST:-chroma}
      CHROMA_HTTP_PORT: ${CHROMA_HTTP_PORT:-8000}
      CHROMA_SSL: ${CHROMA_SSL:-false}
      CHROMA_COLLECTION_NAME: ${CHROMA_COLLECTION_NAME:-agentforge_document_chunks}
      INGESTION_LEASE_SECONDS: ${INGESTION_LEASE_SECONDS:-300}
      INGESTION_RETRY_DELAY_SECONDS: ${INGESTION_RETRY_DELAY_SECONDS:-30}
      INGESTION_WORKER_POLL_INTERVAL_SECONDS: ${INGESTION_WORKER_POLL_INTERVAL_SECONDS:-2}
      INGESTION_REBUILD_BATCH_SIZE: ${INGESTION_REBUILD_BATCH_SIZE:-32}
      CORS_ORIGINS: '${CORS_ORIGINS:-["http://localhost:3000","http://localhost:5173"]}'
    ports:
      - "${API_PORT:-8000}:8000"
    volumes:
      - ./apps/api:/app
      - api_venv:/app/.venv
      - upload_data:/app/data/uploads
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health', timeout=3)"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    restart: unless-stopped
    networks: [backend, frontend]

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: development
    profiles: ["frontend"]
    environment:
      VITE_API_BASE_URL: /api/v1
      VITE_DEV_API_TARGET: http://api:8000
    ports:
      - "${WEB_PORT:-3000}:5173"
    volumes:
      - ./apps/web:/workspace/apps/web
      - ./packages/api-client:/workspace/packages/api-client
      - web_node_modules:/workspace/node_modules
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    networks: [frontend]

networks:
  backend:
    internal: true
  model_egress:
  frontend:

volumes:
  postgres_data:
  chroma_data:
  ollama_data:
  api_venv:
  upload_data:
  web_node_modules:
