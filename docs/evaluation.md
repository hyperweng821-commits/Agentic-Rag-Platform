# Evaluation guide

This document will define golden-dataset format, retrieval metrics, answer metrics, model-judge controls and reproducibility requirements in the evaluation phase.

Evaluation outputs must record model, prompt, chunking and retrieval configuration. Target metrics in `architecture.md` are goals until measured.

